apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: netbird-data
  namespace: netbird
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path # k3s's built-in default storage class
  resources:
    requests:
      storage: 5Gi
